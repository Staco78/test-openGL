#pragma once

#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include <string>
#include "Glew/glew.h"

GLuint LoadShaders(const char* vertex_file_path, const char* fragment_file_path);